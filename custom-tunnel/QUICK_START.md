# ⚡ 빠른 시작 가이드

## 🎯 3분 안에 터널 만들기!

### 1️⃣ 터미널 1: 터널 서버 시작

```bash
cd custom-tunnel/server
npm start
```

✅ 출력 확인:

```
🚀 Custom Tunnel Server Starting...
🌐 터널 서버 실행 중: http://localhost:8080
```

### 2️⃣ 터미널 2: 테스트 서버 시작

```bash
cd custom-tunnel/test-server
npm start
```

✅ 출력 확인:

```
🚀 테스트 서버 실행 중: http://localhost:3000
```

### 3️⃣ 터미널 3: 터널 클라이언트 시작

```bash
cd custom-tunnel/client
npm start 3000
```

✅ 출력 확인:

```
🎉 터널 생성 완료!
📎 터널 URL: http://localhost:8080/abc12345
```

### 4️⃣ 테스트!

브라우저에서 터널 URL 열기:

```
http://localhost:8080/abc12345
```

🎉 **성공!** 아름다운 페이지가 보이면 터널이 작동하고 있습니다!

---

## 🔧 자신의 앱에 사용하기

### React 앱 (CRA)

```bash
# 터미널 1: React 앱 실행
npm start  # 포트 3000에서 실행됨

# 터미널 2: 터널 서버 (한 번만 실행하면 계속 사용 가능)
cd custom-tunnel/server
npm start

# 터미널 3: 터널 클라이언트
cd custom-tunnel/client
npm start 3000
```

### Vite 앱 (React, Vue 등)

```bash
# 터미널 1: Vite 앱 실행
npm run dev  # 포트 5173 (기본값)

# 터미널 2: 터널 서버
cd custom-tunnel/server
npm start

# 터미널 3: 터널 클라이언트
cd custom-tunnel/client
npm start 5173
```

**✨ Vite 설정 변경 불필요!** Host 헤더가 자동으로 localhost로 변경됩니다.

### Next.js 앱

```bash
# 터미널 1: Next.js 앱 실행
npm run dev  # 포트 3000

# 터미널 2: 터널 서버
cd custom-tunnel/server
npm start

# 터미널 3: 터널 클라이언트
cd custom-tunnel/client
npm start 3000
```

### 다른 포트

```bash
# 포트 8080에서 실행 중인 서버
cd custom-tunnel/client
npm start 8080
```

---

## 💡 팁

1. **터널 서버는 한 번만 실행**하면 여러 터널 클라이언트가 동시에 연결 가능
2. **Ctrl+C**로 각 프로세스 종료
3. **로컬에서만 작동** - 현재는 localhost:8080으로만 접근 가능
4. **실제 외부 공개는 VPS 서버 필요** (README.md 참고)

---

## 🐛 문제 해결

### "ECONNREFUSED" 에러

→ 터널 서버를 먼저 실행하세요

### "Tunnel not available" (503)

→ 터널 클라이언트가 실행 중인지 확인

### "Bad Gateway" (502)

→ 로컬 서버(React/Next.js 등)가 실행 중인지 확인

---

## 📚 더 알아보기

- 자세한 설명: [README.md](README.md)
- 작동 원리, 커스터마이징, 배포 방법 등

---

**즐거운 터널링 되세요!** 🚇✨
